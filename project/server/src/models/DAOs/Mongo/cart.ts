import { DBCartClass } from '../../../models/products.interface';
/*
class Cart implements DBCartClass {
    constructor(type: string) {
        if (type === 'Atlas') {
            this.connection = 
        } else {
        }
    }
}s
*/
